# Dependencies of the demo notebook.
#
# PyPowSyBl itself is deliberately NOT listed here: this demo needs the custom
# wheel bundling the Knitro solver (see README.md / BUILDING.md), and pinning
# pypowsybl here would pull the official PyPI build over it.
#
# Install this file FIRST, then the custom wheel.
#
# Last verified with Python 3.12 / pandas 3.0.5 / ipywidgets 8.1.9 /
# pypowsybl-jupyter 1.3.0 / jupyterlab 4.6.3.

pandas
ipywidgets
pypowsybl-jupyter
jupyterlab
